# This file ensures the catalog directory is tracked by Git
# The catalog files (node_list.json, node_info.json) are generated by UpdateNodeCatalog
# and are excluded from Git via .gitignore due to their large size (100MB+)

